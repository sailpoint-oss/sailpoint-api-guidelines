{
  "schemaVersion": 1,
  "ruleId": "avoid-qualifying-verbs",
  "source": {
    "doc": "content/docs/rules/api-contract-and-documentation.mdx",
    "anchor": "#avoid-qualifying-verbs",
    "url": "/docs/rules/api-contract-and-documentation#avoid-qualifying-verbs"
  },
  "currentTitle": "MUST use ASCII camelCase property names",
  "status": "accepted",
  "decision": {
    "changeType": "split",
    "proposedTitle": "MUST use camelCase for JSON property names",
    "proposedText": "**Intent**: keep JSON payloads consistent and predictable across teams and SDKs.\n\nJSON property names MUST use **camelCase**.\n\n**Scope**:\n\n- Applies to JSON request/response bodies.\n- For query parameters and path parameters, use the rules in [#use-camelcase-for-query-parameters] and [#use-lowercase-hyphenated-path-segments-and-camelcase-path-parameters].\n- ASCII-only identifier requirements are defined in [#use-positive-semantics-for-boolean-fields].\n\n**Examples**:\n\n- ✅ `createdAt`, `identityProfileId`, `lastLogin`\n- ❌ `created_at` (snake_case)\n- ❌ `CreatedAt` (PascalCase)\n- ❌ `created-at` (kebab-case)\n\n**Acronyms**:\n\n- Prefer `oauthClientId` over `OAuthClientID`.\n- Be consistent across the entire API surface.\n\n**Testability**:\n\n- OpenAPI schemas use camelCase property names consistently.\n",
    "rationale": "camelCase is widely supported across modern JSON tooling and aligns with common JavaScript/TypeScript conventions used in many client SDKs.",
    "compatNotes": "Renaming fields is breaking. Apply this rule to new APIs/fields; migrate legacy names via new versions or additive aliases only if explicitly supported."
  },
  "reviewNotes": [
    "Split #avoid-qualifying-verbs into two: new #use-positive-semantics-for-boolean-fields for ASCII-only identifiers, and keep #avoid-qualifying-verbs focused on camelCase JSON properties with clear scope."
  ]
}
